import Button from "../../components/Button";

const Home = () => {
  return (
    <>
      <Button onClick={() => alert("Clicky!")}>Click</Button>
      <h1>Home</h1>
    </>
  );
};

export default Home;
